import { DM_Sans } from "next/font/google";
import { Lexend } from "next/font/google";

export const dmSans = DM_Sans({ subsets: ["latin"] });
export const lexend = Lexend({ weight: ["300", "400"], subsets: ["latin"] });
